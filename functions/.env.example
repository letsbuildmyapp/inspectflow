# Copy to functions/.env and fill in
# Anthropic API key — required for live AI summaries.
# If unset, the function returns a deterministic fixture summary so the demo still works.
ANTHROPIC_API_KEY=
